import { useTheme } from '@emotion/react'
import * as RF from 'react-feather'

export const AlertCircle = (props) => {
  const { icons } = useTheme()
  return (
    <RF.AlertCircle size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const AlertTriangle = (props) => {
  const { icons } = useTheme()
  return (
    <RF.AlertTriangle size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const ArrowLeft = (props) => {
  const { icons } = useTheme()
  return <RF.ArrowLeft size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const ArrowRight = (props) => {
  const { icons } = useTheme()
  return (
    <RF.ArrowRight size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const Award = (props) => {
  const { icons } = useTheme()
  return <RF.Award size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Calendar = (props) => {
  const { icons } = useTheme()
  return <RF.Calendar size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const CheckCircle = (props) => {
  const { icons } = useTheme()
  return (
    <RF.CheckCircle size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const ChevronDown = (props) => {
  const { icons } = useTheme()
  return (
    <RF.ChevronDown size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const ChevronLeft = (props) => {
  const { icons } = useTheme()
  return (
    <RF.ChevronLeft size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const ChevronRight = (props) => {
  const { icons } = useTheme()
  return (
    <RF.ChevronRight size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const ChevronUp = (props) => {
  const { icons } = useTheme()
  return <RF.ChevronUp size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Clipboard = (props) => {
  const { icons } = useTheme()
  return <RF.Clipboard size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Clock = (props) => {
  const { icons } = useTheme()
  return <RF.Clock size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Coffee = (props) => {
  const { icons } = useTheme()
  return <RF.Coffee size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const CreditCard = (props) => {
  const { icons } = useTheme()
  return (
    <RF.CreditCard size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const DollarSign = (props) => {
  const { icons } = useTheme()
  return (
    <RF.DollarSign size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const Edit = (props) => {
  const { icons } = useTheme()
  return <RF.Edit size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const ExternalLink = (props) => {
  const { icons } = useTheme()
  return (
    <RF.ExternalLink size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const FileText = (props) => {
  const { icons } = useTheme()
  return <RF.FileText size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Flag = (props) => {
  const { icons } = useTheme()
  return <RF.Flag size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Gift = (props) => {
  const { icons } = useTheme()
  return <RF.Gift size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Grid = (props) => {
  const { icons } = useTheme()
  return <RF.Grid size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Heart = (props) => {
  const { icons } = useTheme()
  return <RF.Heart size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Home = (props) => {
  const { icons } = useTheme()
  return <RF.Home size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const LogOut = (props) => {
  const { icons } = useTheme()
  return <RF.LogOut size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Mail = (props) => {
  const { icons } = useTheme()
  return <RF.Mail size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const MapPin = (props) => {
  const { icons } = useTheme()
  return <RF.MapPin size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Map = (props) => {
  const { icons } = useTheme()
  return <RF.Map size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Menu = (props) => {
  const { icons } = useTheme()
  return <RF.Menu size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Minus = (props) => {
  const { icons } = useTheme()
  return <RF.Minus size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Navigation = (props) => {
  const { icons } = useTheme()
  return (
    <RF.Navigation size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const Phone = (props) => {
  const { icons } = useTheme()
  return <RF.Phone size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Plus = (props) => {
  const { icons } = useTheme()
  return <RF.Plus size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const PlusCircle = (props) => {
  const { icons } = useTheme()
  return (
    <RF.PlusCircle size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const RefreshCw = (props) => {
  const { icons } = useTheme()
  return <RF.RefreshCw size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Save = (props) => {
  const { icons } = useTheme()
  return <RF.Save size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Settings = (props) => {
  const { icons } = useTheme()
  return <RF.Settings size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const ShoppingBag = (props) => {
  const { icons } = useTheme()
  return (
    <RF.ShoppingBag size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const Slash = (props) => {
  const { icons } = useTheme()
  return <RF.Slash size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Sliders = (props) => {
  const { icons } = useTheme()
  return <RF.Sliders size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Smartphone = (props) => {
  const { icons } = useTheme()
  return (
    <RF.Smartphone size={null} strokeWidth={icons.strokeWidth} {...props} />
  )
}

export const Smile = (props) => {
  const { icons } = useTheme()
  return <RF.Smile size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Star = (props) => {
  const { icons } = useTheme()
  return <RF.Star size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Tag = (props) => {
  const { icons } = useTheme()
  return <RF.Tag size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Trash2 = (props) => {
  const { icons } = useTheme()
  return <RF.Trash2 size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Truck = (props) => {
  const { icons } = useTheme()
  return <RF.Truck size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const User = (props) => {
  const { icons } = useTheme()
  return <RF.User size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const Users = (props) => {
  const { icons } = useTheme()
  return <RF.Users size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const UserPlus = (props) => {
  const { icons } = useTheme()
  return <RF.UserPlus size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const UserX = (props) => {
  const { icons } = useTheme()
  return <RF.UserX size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const XCircle = (props) => {
  const { icons } = useTheme()
  return <RF.XCircle size={null} strokeWidth={icons.strokeWidth} {...props} />
}

export const X = (props) => {
  const { icons } = useTheme()
  return <RF.X size={null} strokeWidth={icons.strokeWidth} {...props} />
}
